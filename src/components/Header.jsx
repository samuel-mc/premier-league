import React from 'react';
import '../assets/styles/Header.css'

const Header = ({children}) => (
    <header>
        <ul>
            <li>
            </li>
        </ul>
        {children}
    </header>
)

export { Header };